# Auto-generated by scripts/gen_ft_configs.py - do not edit by hand.
# Closed-task QLoRA FT for gender-referent classification.
# Student: mistralai/Ministral-3-3B-Instruct-2512
# Condition: DAUTH (authentic ambiguous-only - no synthetic masc/fem variants)

base_model: mistralai/Ministral-3-3B-Instruct-2512
chat_template: tokenizer_default

datasets:
  - path: ./ft_data/closed_DAUTH_train.jsonl
    type: chat_template
    system: ""

test_datasets:
  - path: ./ft_data/closed_DAUTH_val.jsonl
    type: chat_template
    split: train
    system: ""

dataset_prepared_path: ./ft_outputs/ft_ministral_3b_DAUTH/prepared

output_dir: ./ft_outputs/ft_ministral_3b_DAUTH

# QLoRA configuration
adapter: qlora
lora_r: 64
lora_alpha: 128
lora_dropout: 0.05
lora_target_modules:
  - q_proj
  - k_proj
  - v_proj
  - o_proj
  - gate_proj
  - up_proj
  - down_proj

# Sequence and packing
sequence_len: 1024
sample_packing: false                  # DDP+LoRA stride bug under packed batches; cheap on 12k rows
eval_sample_packing: false

# 4-bit quantisation
load_in_4bit: true
bnb_config_kwargs:
  bnb_4bit_quant_type: nf4
  bnb_4bit_use_double_quant: true
  bnb_4bit_compute_dtype: bfloat16

# Training
train_on_inputs: false                   # only learn the assistant turn
gradient_accumulation_steps: 2
micro_batch_size: 8
eval_batch_size: 4
num_epochs: 5

dataloader_num_workers: 4
dataloader_pin_memory: true
dataloader_prefetch_factor: 2

# Optimiser
optimizer: adamw_torch
lr_scheduler: cosine
learning_rate: 2.0e-5
weight_decay: 0.01
max_grad_norm: 0.5
cosine_min_lr_ratio: 0.05
warmup_steps: 100

# Mixed precision
bf16: true
tf32: false

# Memory optimisation
gradient_checkpointing: false          # DDP+LoRA reentrant-backward incompat; 4B QLoRA fits on 80GB without it
flash_attention: true

# DDP fix for non-uniform parameter usage across LoRA modules (some Qwen3.5 branches not always active)
ddp_find_unused_parameters: true

# Eval / save / early stopping
eval_strategy: steps
eval_steps: 100
save_strategy: steps
save_steps: 100
logging_steps: 10
early_stopping_patience: 3
load_best_model_at_end: true
metric_for_best_model: eval_loss
greater_is_better: false
save_total_limit: 2

# Loss
label_smoothing_factor: 0.001

# Loss watchdog (catastrophic-spike detection)
loss_watchdog_threshold: 5.0
loss_watchdog_patience: 5

# wandb (optional; left blank by default)
wandb_project:
wandb_entity:
wandb_watch:
wandb_name: ft_ministral_3b_DAUTH
wandb_log_model:
