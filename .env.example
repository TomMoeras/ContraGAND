# Copy to .env and fill in. .env is gitignored.

# Required only if you re-run the Claude generation/evaluation pipeline
# or the Claude classification baseline. Open-source local-model paths do
# NOT need this.
ANTHROPIC_API_KEY=
